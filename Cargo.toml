[package]
name = "skyarch-emu"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "1.25.2", features = ["latest_stable_rust", "extern_crate_alloc"] }
emu-lib = {path = "emu-lib", features = ["rand"]}

[features]
aci = ["emu-lib/aci"]
graphics = ["emu-lib/graphics"]
